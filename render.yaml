services:
  - type: web
    name: rasel-ecommerce
    runtime: python
    buildCommand: "bash build.sh"
    startCommand: "gunicorn config.wsgi:application --chdir backend --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
    preDeployCommand: "python backend/manage.py migrate --noinput"
    envVars:
      - key: DEBUG
        value: "0"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: SITE_URL
        value: "https://rasel-ecommerce.onrender.com"
      - key: PYTHON_VERSION
        value: "3.11"
      - key: MP_ACCESS_TOKEN
        sync: false
      - key: DJANGO_SETTINGS_MODULE
        value: "config.settings"
      - key: DATABASE_URL
        fromDatabase:
          name: rasel-db
          property: connectionString

databases:
  - name: rasel-db
    databaseName: rasel
    user: rasel
    plan: free
